# Agent Instructions

- Use only the `hoantran1107` GitHub and Vercel accounts for this repository. Do not switch to, deploy from, or authenticate as `hoan.trankhai` or any Infodation account.
- Before running commands that can require browser confirmation, such as `gh auth refresh`, `gh auth login`, `vercel login`, or account switching commands, check whether the operation is interactive. If it needs the user's browser action, stop and ask the user to complete the login instead of waiting indefinitely.
- If an auth command is accidentally left waiting for user action, stop the process, report what happened, and ask the user before retrying.
